# Что дальше

Отмечено, что уже сделано. Предложения и PR приветствуются.

## Сделано

- [x] `gui-test.ps1`: запуск по имени профиля базы, веб и толстый/тонкий клиент
- [x] Регрессионные наборы по базам (`tests/<База>/`, режим `test` навыка web-test): шаги, проверки, теги, фильтры, повторы, видео
- [x] Отчет Allure 3 одним HTML-файлом, на русском, с категориями падений и сведениями о стенде (без Java)
- [x] `-Init` — заготовка набора для новой базы; `-Suite` — все JSON-сценарии толстого клиента базы (`"url"` внутри сценария)
- [x] Хелперы для тестов и разовых сценариев: файл через диалог 1С, HTML-поля, отрисовка картинки, поиск в динамическом списке, пометка на удаление
- [x] Проверка стенда перед прогоном (`prepare()`: публикация отвечает)
- [x] Исправления навыков upstream: выбор главного окна клиента, скриншот ошибки в папке артефактов (см. [NOTICE.md](NOTICE.md))
- [x] Документация: workflow, регресс, рецепты, грабли ([docs/](docs/))

## Ближайшее

- [ ] **Изолированные данные.** Восстановление отдельной тестовой базы из эталона (`.dt`) в `prepare()` по флагу `-- --rebuild-stand`, чтобы прогоны не зависели друг от друга.
- [ ] **Проверки отчетов СКД** живым прогоном (`readSpreadsheet`) и рецепт сверки итогов с контрольными значениями.
- [ ] **Хелперы для табличных частей**: добавить строку, заполнить, проверить итог по колонке.
- [ ] **Вход через форму авторизации** веб-клиента, чтобы не прописывать пользователя в `default.vrd`.
- [ ] **Отчет толстого клиента в Allure** — чтобы оба режима попадали в один отчет.

## Дальше

- [ ] Запуск по расписанию (планировщик Windows) с уведомлением о красном прогоне.
- [ ] Многопользовательские тесты (процессы согласования) — движок умеет (`contexts`), нужен пример и рецепт по лицензиям.
- [ ] Запись теста по действиям человека: кликаешь в браузере — получаешь черновик `.test.mjs`.
- [ ] История прогонов в Allure (тренды, «нестабильные» тесты).
