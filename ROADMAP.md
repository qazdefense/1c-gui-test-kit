# Что дальше

Отмечено, что уже сделано. Предложения и PR приветствуются.

## Сделано

- [x] Обертка `gui-test.ps1`: запуск по имени профиля базы, оба режима (веб и толстый/тонкий клиент)
- [x] Библиотека хелперов `lib/1c-helpers.js`: загрузка файла через диалог 1С, поиск HTML-полей, проверка отрисовки картинки, поиск в динамическом списке, пометка на удаление
- [x] Режим набора `-Suite`: все сценарии папки, сводная таблица, `suite.json`, общий код возврата
- [x] Исправления навыков upstream: выбор главного окна клиента, скриншот ошибки в папке артефактов (см. [NOTICE.md](NOTICE.md))
- [x] Описание граблей веб-клиента и рецептов ([docs/](docs/))

## Ближайшее

- [ ] **Изолированные данные.** Перед набором — восстановление файловой тестовой базы из эталонной копии (или `.dt`), вместо уборки каждым сценарием. Прогоны не зависят друг от друга, упавший сценарий не оставляет мусор.
- [ ] **Отчет о прогоне в HTML.** Одна страница: сценарии, шаги, время, скриншоты, ошибки 1С — чтобы приложить к задаче или отправить в чат.
- [ ] **Видео прогона.** У навыка web-test есть запись (`startRecording`, нужен ffmpeg) — включать флагом `-Record` и класть mp4 к артефактам.
- [ ] **Проверки отчетов СКД** живым прогоном (`readSpreadsheet`), рецепт сверки итогов с контрольными значениями.
- [ ] **Вход через форму авторизации** веб-клиента, чтобы не прописывать пользователя в `default.vrd`.
- [ ] **Хелперы для табличных частей**: добавить строку, заполнить, проверить итог по колонке.

## Дальше

- [ ] Запуск по расписанию (планировщик Windows) на ночной сборке с уведомлением о красном прогоне.
- [ ] Запись сценария по действиям человека: кликаешь в браузере — получаешь черновик `.js`.
- [ ] Регрессионный режим web-test (`*.test.mjs`, Allure-отчет) через ту же обертку.
- [ ] Проверка после обновления конфигурации: набор прогоняется автоматически после `UpdateDBCfg`.
